This collection of katas provides exercises for practicing regular expressions.

Each kata is an NUnit test fixture containing parameterized tests. Complete each exercise by defining a pattern that passes all of its TestCases. You might also need to supply RegexOptions to the Regex constructor.